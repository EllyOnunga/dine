[build]
  # Build the Vite React frontend
  command = "npm install && npx vite build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
